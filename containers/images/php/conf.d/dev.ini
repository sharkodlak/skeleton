display_errors=On
display_startup_errors=On
log_errors=On
error_reporting=E_ALL

; The working tree is bind-mounted in development, so edits must take effect on
; the next request rather than up to opcache.revalidate_freq seconds later.
opcache.revalidate_freq=0
