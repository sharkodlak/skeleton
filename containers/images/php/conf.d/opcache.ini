opcache.enable=1
opcache.memory_consumption=256
opcache.max_accelerated_files=20000

; Timestamps stay validated so a deploy that swaps files in place is picked up
; without a restart. Production images that never change on disk can turn this
; off for a further speed-up.
opcache.validate_timestamps=1
opcache.revalidate_freq=2
